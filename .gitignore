.DS_Store

# 아이폰 원본 영상(HEVC, 545MB) — 보관용이라 올리지 않음.
# 페이지는 영상/ 폴더의 변환본을 씁니다.
# (맥에서는 대소문자를 안 가려서 *.MP4 로 쓰면 영상/*.mp4 까지 걸립니다. 최상단만 지정)
/*.MP4

# 예전에 시도했던 벡터 도안 초안
도안/

# 임시 파일
_*.html

# 다듬기 전 원본 (19MB). 내 맥에만 두고 웹에는 올리지 않는다.
# 도안다듬기.py 가 이 폴더를 기준으로 다시 다듬으므로 지우지 말 것.
도안원본/

# 손그림 사진 원본 (릴스 캡처)
손그림/
